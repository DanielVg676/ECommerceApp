import { React } from "react";
import { View, Text } from 'react-native';

const ProductList = () => {
    return (
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
            <Text>Pantalla de lista de productos</Text>
        </View>
    )
}

export default ProductList;